package _01defineAndUseClosures

/*
Now that you have a basic idea of what closure is
 a very common question is why we should not just use lambdas?

 Because you can do everything that lambda does via closures
    but there are things that closures can do that lambda cannot do - Making DSLs
 */